(mat a () ...)
(isolate-mat a () ...)
(really-complicated-mat a () ...)
(define asdf (system-mat xxx))

(define (math-function) ...)
(math-server:go)

(match a ...)
(match-let* ...)
(display matcher)

(let ([a ...]))
(let-values ([(a b) ...]) ...)

(set! danger)
(hashtable-set! danger)
(library:set! internal-danger)
(let ([a (hashtable-set-and-get! danger)]) ...)

(define-syntax (name x) ...)
(define-record <assay> a
  b
  c)

(define definitions '(trace-define not-a-trace))
