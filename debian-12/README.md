# Debian 12 Bookworm Dotfiles

Dotfiles and scripts.

## Installation

- Follow each directory's instructions.
- Install links to scripts: `./install.sh`
- Re-start shell, so that local scripts get added to your `PATH`.
