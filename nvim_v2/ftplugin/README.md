# `ftplugin`

filetype plugins, which are automatically run when a buffer is associated with
a filetype.

See `:h filetype-plugin` for details.

See `:setfiletype <ctrl-d>` for a list of known filetypes.
